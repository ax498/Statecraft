---
description: 
alwaysApply: true
---

---
description: 
alwaysApply: true
---

# 项目文档规范与工具指南

## 核心原则
1. **文档完整性**  
   所有核心设计文档必须包含 YAML 头部 (Status / Version / Last_Sync / Ref_Scripts)

2. **代码辅助性**  
   核心脚本必须添加 `@Spec` 注释链接到文档

3. **技术索引**  
   文档尾部必须包含“技术索引”（核心脚本、关键方法、数据结构、枚举/常量）

4. **常数一致性**  
   `PlayerStatsService.gd` 和 `StatCalculator.gd` 的属性常数必须完全一致

---

## 快速工具
- **定期验证**：运行 `/doc-code-sync` 检查文档与代码对齐度  
- **常数验证**：运行 `python scripts/tools/check_constants_consistency.py` 验证属性常数一致性  
- **经验捕捉**：运行 `/experience-capture` 记录玩家经验  
- **项目进度**：运行 `/progress-update` 同步更新项目进度（设计文档 README + 项目管理 README）  

---

## 文档存放规范 (V2 重构)

| 文档类型 | 存放位置 |
| -------- | -------- |
| 核心文档 / Buff / 常量 | 项目管理/10_设计文档/10_核心引擎/ |
| 技术文档 | 项目管理/10_设计文档/03_数据与同步/$13.7_施加点/数据结构/ |
| 经验文档 | 项目管理/10_设计文档/03_数据与同步/经验捕捉/ |
| 工具文档 | 项目管理/10_设计文档/60_工具/ |
| UI 文档 | 项目管理/10_设计文档/60_UI/ |
| 项目进度 | 项目管理/20_开发日志/项目进度/工具使用指南/ |
| 版本记录 | 项目管理/20_开发日志/版本记录/ |
| 设计文档 | 项目管理/10_设计文档/20_开发计划/ |
| 时间记录 | 项目管理/20_开发日志/时间记录/YYYY-MM-DD |
| 玩法文档 | 项目管理/10_设计文档/60_玩法扩展/ |
| 垃圾箱 (过时文档) | 项目管理/0_制作人工作台/99_垃圾箱/ |

---

## 使用说明
- 所有文档必须遵循上述存放路径，避免混乱  
- 修改核心文档需谨慎，保持版本记录完整  
- 定期运行工具，确保文档与代码保持一致性  


# Statecraft | Godot 4.6.1 | GDScript | 卡片对决游戏

请尽可能用中文和我交流！！！

---

# 🚫 禁止（红线）

- 禁止生成 Godot 3.x 代码
- UI 脚本禁止包含游戏逻辑（仅反映状态）
- 禁止擅自修改核心架构文档
- 🔒 禁止未经确认修改 `10_核心引擎/` 下的战斗公式（改动前必须说明影响范围）
- 禁止生成额外文档（如 summary.md/plan.txt）除非明确要求
- 禁止生成测试脚本，除非明确要求
- 代码优先，小改动不要长篇计划书

# 🏗️ 架构规则

- 数据通过 Manager 访问 (PlayerManager/CardManager/BattleManager/WorldTimer)
- 禁止在 `_ready()` 交叉引用 Manager，改用 `init_data()` 或信号
- 组合 > 继承，用组件节点避免深层后缀
- UI 信号驱动：禁止在 `_process()` 查询数据，必须监听信号
- 物品/卡牌/敌人用 .tres Resource，禁止硬编码；存档只存 ID
- 可复用元素必须是 .tscn 场景，禁止代码创建 UI
- **以编辑器为准**：.tscn 定义布局与可见状态，脚本仅做运行时辅助（显隐、动画、逻辑），不重写布局
- 数据流：直接编辑 .tres -> GameDatabase (已废弃 CSV 导入流程)

# 📜 GDScript 4.x 类型推断规则（重要）

- **谨慎使用 `:=`**：`:=` 是“类型推断赋值”。当右侧表达式可能是 `Variant`（例如 `max()` / `min()`、某些内置方法返回值）时，避免推断失败。
- **推荐写法**：对局部变量直接写显式类型，避免推断失败：
    - `var available: int = max(1, container_width - total_gap_width)`
    - `var extra: int = available - base_w * slot_count`
- **只有在类型 100% 明确时才用 `:=`**：
    - 例如 `var n := 1`、`var s := "abc"`、`var v := Vector2(1, 2)`、`var node := $SomeNode as Button`
- **混合类型运算必须强制转换**：
    - 例如 `int(floor(float(a) / float(b)))`，不要依赖推断。
- **Windows 兼容性**：避免强类型容器（`Dictionary[K, V]`）跨模块传递；枚举比较前检查类型；详见项目规范。

# 📋 日志规范

- **GameLogger**：使用 `GameLogger.record(tag, action, data)` 记录关键流程，每局自动保存到 `user://logs/game_YYYYMMDD_HHMMSS.log`
- **默认不移除**：为排查 bug 添加的日志默认保留，方便以后排查问题；仅在确认无用时再移除
- 关键流程（重新开始、回合切换、结算、Mod 效果）必须打日志

# 💬 命名规范与注释

- 命名规范强制：文件和文件夹必须使用 `snake_case`（如 `card_data.gd`）；类名和场景节点必须使用 `PascalCase`
- 关键代码必须中文注释（复杂计算、Manager 初始化、信号逻辑、状态转换）
- **代码文档规范**：编写或修改代码时，须为每个脚本编写或更新对应的 MD 文档，说明：① 主要逻辑；② 各方法的功能与实现方式。文档存放于 `项目管理/10_设计文档/10_核心引擎/代码文档/`，文件名与脚本名对应（如 `card_manager.gd` → `card_manager.md`）。

# 🎫 TODO 标签规范

- **临时硬编码**：必须标注 `# TODO: 硬编码 - [原因/未来计划]`
- **占位功能**：（未来要开发的功能）必须标注 `# TODO: 占位 - [功能描述]`
- **待修复问题**：使用 `# FIXME: [问题描述]`
- **需要优化**：使用 `# TODO: 优化 - [优化方向]`
- 标签格式统一便于 `/todo-sync` 插件识别
- **用户待办**：用户说“待办”、“TODO”、“记一下”时，写入 `项目管理/0_制作人工作台/者待办记事本.md`

# 🎴 卡牌开发规则

- **默认游戏内卡牌**：除非特殊交代，新卡牌一律写成游戏内卡牌（`scripts/cards/card_xxx.gd` + 加入 `card_registry.gd`），不要写成 Mod。
- **Mod 卡牌**：仅当明确要求「Mod 卡牌」或「扩展卡牌」时，才使用 `mods/xxx_mod/` + `register_card`。

# 🎴 Mod 卡牌默认规则

编写 Mod 卡牌时，`register_card` 默认行为（除非显式覆盖）：

- **数量**：默认 4 张（可通过 `count` 覆盖）
- **分值**：默认 1、2、3、4 各一张（可通过 `score` 或 `scores` 覆盖）
- **矛盾判定**：默认参与同名矛盾判定（`count_towards_contradiction: true`，可设为 `false` 排除）
- **回合结束**：默认不强制结束回合（`force_ends_turn: false`），效果触发后正常进行游戏；除非矛盾或显式指定 `force_ends_turn: true`
- **效果触发后不结算本回合**：默认效果触发后不结算，卡牌留在回合区，玩家可继续抽牌；需结算时显式调用 `mod_settle_and_continue`
- **触发效果的卡牌不可消失**：任何卡牌触发效果后不能消失，必须保证正常结算；除非有特殊交代（如被移除的目标卡牌）
- **联机模式必须考虑真人对手**：写卡牌 Mod 时，联机模式下对手是真人而非 AI。需实现：① 客户端抽到需选择的卡时，主机通过 `request_mod_xxx_to_client` 通知客户端触发 `post_draw_animation`；② 客户端选择完成后通过 RPC 将选择发送给主机执行；③ 主机执行后通过 RPC 同步特效到客户端。参考他山之石 Mod 与 BattleManager 的 `mod_tashanzhishi_pending`、`request_tashanzhishi_selection`、`_rpc_mod_soft_remove_effect` 等实现。

示例：`{"name": "他山之石", "ability_id": "tashanzhishi", "scores": [1,2,3,4]}` 即 4 张、1-4 分、参与矛盾、不强制结束回合、触发后不结算。

# 🎬 特效阻塞规则（重要）

- **所有特效播放时必须阻塞其他运算**：特效播放期间，后续逻辑不得执行；只有特效播放完毕后，才能进行下一步代码
- 使用 `tween.tween_callback` 在特效结束时执行下一步，或 `await tween.finished`
- 禁止在发出特效的同时立即执行 settle/next_turn 等会改变游戏状态的逻辑
- 适用范围：卡牌抽牌动画、卡牌专属特效、九牛一毛夺取飞牌、收牌动画、矛盾震动、Mod 特效等

# 📂 文件组织与代码结构

- **文档规范**：所有文档存放在 `项目管理/` 下对应文件夹，**禁止放在一级目录下**
- **文档开头**：必须包含 `**版本**：vX.X.X`（取自 project settings）和 `**最后更新**：YYYY-MM-DD`
- **文件版本号管理规则**：格式 `[游戏版本]-[该版本期间的第几版]`（如 `v0.4.2-1`），版本号与 `project.godot` 同步
- **代码结构**：
    - `scenes/main/`（主场景）、`scenes/ui/`（UI 界面）
    - `scripts/autoload/`（单例）、`scripts/components/`（组件）
    - `resources/`（.tres 资源文件）

# 🔐 协议与隐私文案维护

- **协议文件固定路径**：`resources/legal/user_agreement.txt`、`resources/legal/privacy_policy.txt`
- **移动端兜底脚本**：`scripts/data/legal_documents.gd` 内置了一份移动端兜底协议文案；修改上述两个协议文件时，必须同步检查这里的内容是否需要更新
- **同步检查规则**：凡是新增或调整登录、账号体系、第三方 SDK、联网联机、数据采集、日志上报、存档同步、客服反馈、未成年人保护、广告、付费、活动规则等内容时，必须同步检查这两个文件是否需要更新
- **UI 一致性**：若协议或隐私政策内容、标题或生效日期有变化，需同时检查开始界面的协议提示文案与弹窗展示是否需要同步调整
- **TapTap 权限对齐**：若 Android / TapTap 版本保留局域网联机能力，提审前必须检查 TapTap 后台权限说明已勾选“网络权限”，用途说明统一为“用于局域网对决数据同步”


